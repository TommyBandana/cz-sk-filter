services:
  - type: web
    name: cz-sk-filter
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: TORRENTIO_URL
        value: https://torrentio.strem.fun
